---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  labels:
    openmcp.cloud/cluster: platform
  name: e2etestspecifications.test-runner.openmcp.cloud
spec:
  group: test-runner.openmcp.cloud
  names:
    kind: E2ETestSpecification
    listKind: E2ETestSpecificationList
    plural: e2etestspecifications
    singular: e2etestspecification
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: E2ETestSpecification is the Schema for the e2etestspecifications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of E2ETestSpecification
            properties:
              schedule:
                default: '@daily'
                type: string
              testCases:
                description: TestCases is a list of test cases to be executed in order.
                  Each test case includes a name and optional configuration.
                items:
                  properties:
                    config:
                      additionalProperties:
                        type: string
                      type: object
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - testCases
            type: object
          status:
            description: status defines the observed state of E2ETestSpecification
            properties:
              lastExecutionTime:
                format: date-time
                type: string
            required:
            - lastExecutionTime
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
